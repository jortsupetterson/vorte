# Keep it minimal but self explanatory

- `Any` type is forbidden. Each variable must resolve to a specific type in IntelliSense.
- Descriptions must be short. One line only.
- `@returns {void}` must include a brief note describing the side-effect.
- English is the only allowed language.
