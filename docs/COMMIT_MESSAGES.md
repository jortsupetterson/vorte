# Commit Message Rules

- Keep every commit message short and single-line.
- No explanations, no story — just what changed and where.
- Always include the path, so the history stays readable.

## Example

git commit -m "Refactored DateWheel logic at /src/MainScripts/customElements/DateWheel/\*"
