export default css`
  nav ul li button#home {
    background: var(--contentGhostColor);
    width: 5rem;
    border-radius: 0.2rem;
  }
  nav ul li button#home svg {
    fill: rgb(from var(--contentColor) r g b / 0.4);
  }
`;
