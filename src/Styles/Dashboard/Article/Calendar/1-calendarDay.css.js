export default css`
  article#calendar_day #calendarDisplay {
    display: flex;
    flex-direction: row;
    padding: 0.5rem;
    border-radius: 0.5rem;
    background: var(--accentGhostColor);
    min-height: 70%;
  }
`;
